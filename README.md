# Jobs
I created a Job class in the model folder that contains JSON file properties and I created a helper class called JobList to fetch the JSON file.
I have a controller called JobController which implements the logic and sorting and filtering and passes the data to the view.
